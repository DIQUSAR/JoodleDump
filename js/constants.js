// КОНФИГУРАЦИЯ ИГРЫ
const CONFIG = {
    W: 380,           // Ширина холста
    H: 620,           // Высота холста
    GRAVITY: 0.35,    // Сила гравитации
    JUMP_V: -13.5,    // Скорость обычного прыжка (отрицательная = вверх)
    PLAYER_SPD: 4.2,    // Горизонтальная скорость игрока
    GRID: 20          // Размер ячейки сетки фона
};

const { W, H, GRAVITY, JUMP_V, PLAYER_SPD, GRID } = CONFIG;
